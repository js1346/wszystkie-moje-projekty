#include <bits/stdc++.h>
#include <iostream>

using namespace std;
 
  int f[1000005];
  int sumppp[1000005];
  
int main()
{
    for (int i=0;  i<1000005; i++) f[i]=0;
   for (int i=2;  i<500005; i++){
            for(int k=2*i; k<=1000002;k += i)f[k]++;
       }
   
  // for(int i=2; i<50; i++) cout<<f[i]<<" ";cout<<"\n"; //potad cudnie jest
   
   sumppp[0]=0;
   for(int i=1; i<1000002;i++){
       sumppp[i]=sumppp[i-1];
      if (f[i]==2) sumppp[i]++;
   }
    //for(int i=2; i<50; i++) cout<<sumppp[i]%10<<" ";
 
   int a; int b; int z; int maczolix;int  maxi;
   cin>>z;
  vector<int> wynik(z+3);
   for (int i=0; i<z; i++){
       cin>>a; cin>>b;
       wynik[i]=sumppp[b]-sumppp[a-1];
       //cout<<sumppp[b]<<" "<<sumppp[a-1]<<endl;
       /*maxi=0;maczolix=0;
       for(int i=a;i<=b;i++){
           if(sumppp[i]>sumppp[i-1]) maczolix++;
           else {maxi=max(maxi,maczolix);maczolix=0;}
       }
       maxi=max(maxi,maczolix);
       wynik[i]=maxi;*/ 
   }

   for(int i=0;i<z;i++) cout<<wynik[i]<<endl;
    return 0;
}
