#include<bits/stdc++.h>
using namespace std;
int main()
    {
        long n; cin>>n; long maxlok=0;long minlok=0;long minimum;long wynik=0;long x;
        vector <long>a(n);vector <long>l(n); vector <long>p(n);  
        for(int i=0;i<n;i++) cin>>a[i];
        for(int i=1;i<n-1;i++) 
            {
                maxlok=max(a[i],maxlok+a[i]);
                l[i]=maxlok;
                //cout<<l[i]<<endl;
            } 
         for(int i=n-2;i>0;i--) 
            {
                minlok=max(a[i],minlok+a[i]);
                p[i]=minlok;
               // cout<<p[i];
            }  
          for(int i=2;i<n-2;i++) 
            {x=l[i-1]+p[i+1];
               wynik=max(x,wynik);
            } 
        cout<<wynik;
        return 0;
    }
